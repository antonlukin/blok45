apiVersion: v1
kind: ConfigMap
metadata:
  name: blok45-nginx
data:
  default.conf: |
    server {
      listen 80;
      server_name _;
      root /var/www/html;

      index index.php index.html;

      location / {
        try_files $uri $uri/ /index.php?$args;
      }

      location ~ \.php$ {
        try_files $uri =404;

        include fastcgi_params;
        fastcgi_param SCRIPT_FILENAME $document_root$fastcgi_script_name;
        fastcgi_param HTTPS off;
        fastcgi_pass 127.0.0.1:9000;
      }

      location ~* \.(css|js|png|jpg|jpeg|gif|svg|ico|woff2?)$ {
        expires 7d;
        access_log off;
      }

      location = /xmlrpc.php {
        deny all;
      }
    }

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: blok45
spec:
  replicas: 2
  strategy:
    type: RollingUpdate
    rollingUpdate:
      maxSurge: 1
      maxUnavailable: 0
  selector:
    matchLabels:
      app: blok45
  template:
    metadata:
      labels:
        app: blok45
    spec:
      volumes:
        - name: wp
          emptyDir: {}
        - name: nginx-conf
          configMap:
            name: blok45-nginx

      initContainers:
        - name: wp-seed
          image: ghcr.io/antonlukin/blok45:latest
          imagePullPolicy: Always
          command: ["sh", "-lc"]
          args:
            - |
              set -e
              cp -a /usr/src/wordpress/. /work/
          volumeMounts:
            - name: wp
              mountPath: /work

      containers:
        - name: php-fpm
          image: ghcr.io/antonlukin/blok45:latest
          imagePullPolicy: Always
          env:
            - name: WORDPRESS_CONFIG_EXTRA
              value: |
                if (file_exists(__DIR__ . '/vendor/autoload.php')) {
                  require_once __DIR__ . '/vendor/autoload.php';
                }

                define('WP_HOME', 'https://blok45.art');
                define('WP_SITEURL', 'https://blok45.art');

                define('DISALLOW_FILE_EDIT', true);
                define('DISALLOW_FILE_MODS', true);
                define('UMAMI_WEBSITE_ID', '5ef9ddb0-8aa6-45f6-8058-2eaefc49f643');
                define('DISABLE_WP_CRON', true);

                define('S3_UPLOADS_KEY', $_ENV['S3_UPLOADS_KEY'] ?? null);
                define('S3_UPLOADS_SECRET', $_ENV['S3_UPLOADS_SECRET'] ?? null);
                define('S3_UPLOADS_BUCKET', $_ENV['S3_UPLOADS_BUCKET'] ?? null);
                define('S3_UPLOADS_BUCKET_URL', $_ENV['S3_UPLOADS_BUCKET_URL'] ?? null);
                define('S3_UPLOADS_REGION', $_ENV['S3_UPLOADS_REGION'] ?? null);
                define('S3_UPLOADS_USE_PATH_STYLE_ENDPOINT', true);

                define('CLOUDFLARE_ZONE_ID', $_ENV['CLOUDFLARE_ZONE_ID'] ?? null);
                define('CLOUDFLARE_API_TOKEN', $_ENV['CLOUDFLARE_API_TOKEN'] ?? null);

                define('BLOK45_LOGIN_SLUG', $_ENV['BLOK45_LOGIN_SLUG'] ?? null);
                define('BLOK45_LOGIN_REDIRECT_SLUG', $_ENV['BLOK45_LOGIN_REDIRECT_SLUG'] ?? null);
          envFrom:
            - secretRef:
                name: blok45-db
            - secretRef:
                name: blok45-wp-secrets
          ports:
            - containerPort: 9000
          volumeMounts:
            - name: wp
              mountPath: /var/www/html

        - name: nginx
          image: nginx:1.27-alpine
          ports:
            - containerPort: 80
          volumeMounts:
            - name: wp
              mountPath: /var/www/html
            - name: nginx-conf
              mountPath: /etc/nginx/conf.d/default.conf
              subPath: default.conf

---
apiVersion: v1
kind: Service
metadata:
  name: blok45
spec:
  selector:
    app: blok45
  ports:
    - name: http
      port: 80
      targetPort: 80

---
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: blok45
  annotations:
    nginx.ingress.kubernetes.io/proxy-body-size: "64m"
    nginx.ingress.kubernetes.io/force-ssl-redirect: "true"
    cert-manager.io/cluster-issuer: letsencrypt-prod
spec:
  ingressClassName: nginx
  tls:
    - hosts:
        - blok45.art
      secretName: blok45-tls
  rules:
    - host: blok45.art
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: blok45
                port:
                  number: 80

---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: redis
spec:
  replicas: 1
  selector:
    matchLabels: { app: redis }
  template:
    metadata:
      labels: { app: redis }
    spec:
      containers:
        - name: redis
          image: redis:7-alpine
          ports:
            - containerPort: 6379

---
apiVersion: v1
kind: Service
metadata:
  name: redis
spec:
  selector: { app: redis }
  ports:
    - name: redis
      port: 6379
      targetPort: 6379